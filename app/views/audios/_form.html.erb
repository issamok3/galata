<%= simple_form_for @audio do |f| %>
  <%= f.association :site, label: 'Pick a site' %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :spotify_uri, label: 'Paste the Spotify URI below' %>
  <%= f.button :submit, class: 'btn-green' %>
<% end %>
