<% @audios.each do |audio| %>
    <div class="card-product">
    <i class="fas fa-headphones"></i>
      <div class="card-product-infos">
        <%= audio.title %>
         <%= link_to "See reviews", content_reviews_path(audio)  %>
      </div>
    </div>
<% end %>
