
<div class="container">
    <div class="loader d-none">
      <span></span>
      <span></span>
      <span></span>
      <span></span>
      <span></span>
    </div>
  <div id="map"
    style="width: 100%; height: 100vh;"
    data-markers="<%= @site_markers.to_json %>"
    data-usermarkers="<%= @user_markers.to_json %>"
    data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
    <%= render "filter_by_category" %>
</div>
