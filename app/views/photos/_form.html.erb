<%= simple_form_for @photo do |f| %>
  <%= f.association :site, label: 'Pick a site' %>
  <%= f.input :title  %>
  <%= f.input :description %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>